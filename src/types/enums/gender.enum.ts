export enum Gender {
    Male    = 'Male',
    Female  = 'Female',
    Other   = 'Other',
    Unknown = 'Unknown',
}

export const GenderList: Gender[] = [Gender.Male, Gender.Female, Gender.Other, Gender.Unknown];
